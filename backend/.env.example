# Copy this to .env and fill values for local development or production
# Local MongoDB (Docker): mongodb://mongo:27017/TaskManager
# Example Atlas connection (replace <user> and <password>):
MONGO_URI=mongodb+srv://samikshaprathip28_db_user:<samiksha>@cluster0.jfjtnvo.mongodb.net/TaskManager
# Or use Atlas connection string:
# MONGO_URI="mongodb+srv://<user>:<password>@cluster0.jfjtnvo.mongodb.net/TaskManager?retryWrites=true&w=majority"

JWT_SECRET=your_jwt_secret_here
PORT=4000

# Optional SMTP settings for sending invmongodb+srv://samikshaprathip28_db_user:<db_password>@cluster0.jfjtnvo.mongodb.net/?appName=Cluster0ite emails (Mailtrap for testing)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=no-reply@example.com

# Frontend origin used in invite links
FRONTEND_ORIGIN=http://localhost:5173
